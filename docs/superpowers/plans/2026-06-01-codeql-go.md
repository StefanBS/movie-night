# CodeQL Go (add to matrix) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:subagent-driven-development (recommended) or superpowers-extended-cc:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extend `.github/workflows/codeql.yml` to analyze Go (backend) alongside the existing JavaScript/TypeScript, via a CodeQL language matrix.

**Architecture:** Convert the single `analyze` job into a matrix `[javascript-typescript (build-mode none), go (build-mode manual)]`. The Go leg adds `setup-go` + a traced `go build ./...` in `backend/` (the module is in a subdir, so autobuild-from-root won't work); the JS/TS leg is unchanged. gosec stays (complementary).

**Tech Stack:** GitHub Actions; `github/codeql-action/{init,analyze}@84498526…` (codeql-bundle-v2.25.5); `actions/setup-go@4a3601…` (v6.4.0).

**Spec:** `docs/superpowers/specs/2026-06-01-codeql-go-design.md`

**Branch:** `feat/ci-security-gate`.

---

### Task 1: Add Go to the CodeQL matrix in `codeql.yml`

**Goal:** `codeql.yml` runs CodeQL for both `javascript-typescript` and `go`.

**Files:**
- Modify: `.github/workflows/codeql.yml` (full replace)
- Reference only (do NOT modify): `.github/workflows/backend.yml` (setup-go pin), `backend/go.mod`/`go.sum`.

**Acceptance Criteria:**
- [ ] `analyze` job uses a `strategy.matrix.include` with `javascript-typescript` (`build-mode: none`) and `go` (`build-mode: manual`), plus `fail-fast: false`.
- [ ] `setup-go` step and a `working-directory: backend` `go build ./...` step are both gated `if: matrix.language == 'go'`; the build step sits **between** `init` and `analyze`.
- [ ] `init` uses `languages: ${{ matrix.language }}` + `build-mode: ${{ matrix.build-mode }}`; `analyze` uses `category: "/language:${{ matrix.language }}"`.
- [ ] Triggers, top-level `permissions`, `concurrency`, and job-level permissions unchanged; all `uses:` SHA-pinned (checkout `de0fac2e…`, setup-go `4a3601…`, codeql-action `84498526…` ×2).
- [ ] Header comment updated to mention JS/TS + Go (gosec complementary).

**Verify:**
- `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/codeql.yml')); j=d['jobs']['analyze']; langs={m['language']:m['build-mode'] for m in j['strategy']['matrix']['include']}; assert langs=={'javascript-typescript':'none','go':'manual'}, langs; assert j['strategy']['fail-fast'] is False; print('matrix ok')"` → `matrix ok`.
- `grep -c "84498526a009a99c875e83ef4821a8ba52de7c22" .github/workflows/codeql.yml` → `2`.
- `grep -c "matrix.language == 'go'" .github/workflows/codeql.yml` → `2`.

**Steps:**

- [ ] **Step 1: Replace `.github/workflows/codeql.yml` in full**

```yaml
name: codeql

# CodeQL static analysis covering the mobile JS/TS code and the backend Go code
# (complementary to gosec's Go-idiom rules — both are kept). No path filter so
# the checks always report (safe to require in branch protection).
on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1" # Mondays 06:00 UTC

permissions:
  contents: read

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: analyze
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: javascript-typescript
            build-mode: none
          - language: go
            build-mode: manual
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # Go module lives in backend/; install the toolchain for the manual build.
      - if: matrix.language == 'go'
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: backend/go.mod
          cache-dependency-path: backend/go.sum

      - name: Initialize CodeQL
        uses: github/codeql-action/init@84498526a009a99c875e83ef4821a8ba52de7c22 # codeql-bundle-v2.25.5
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      # Manual build for Go: the module is in backend/, not the repo root, so
      # autobuild-from-root would fail. CodeQL traces this compile.
      - if: matrix.language == 'go'
        name: Build Go (traced by CodeQL)
        working-directory: backend
        run: go build ./...

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@84498526a009a99c875e83ef4821a8ba52de7c22 # codeql-bundle-v2.25.5
        with:
          category: "/language:${{ matrix.language }}"
```

- [ ] **Step 2: Validate**

Run:
```bash
python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/codeql.yml')); j=d['jobs']['analyze']; langs={m['language']:m['build-mode'] for m in j['strategy']['matrix']['include']}; assert langs=={'javascript-typescript':'none','go':'manual'}, langs; assert j['strategy']['fail-fast'] is False; print('matrix ok')"
grep -c "84498526a009a99c875e83ef4821a8ba52de7c22" .github/workflows/codeql.yml   # expect 2
grep -c "matrix.language == 'go'" .github/workflows/codeql.yml                     # expect 2
```
Expected: `matrix ok`, `2`, `2`.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/codeql.yml
git commit -m "feat: add Go to CodeQL analysis (matrix with manual build)"
```
(The lefthook pre-commit betterleaks scan runs and must pass.)
