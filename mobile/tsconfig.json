{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // TypeScript 6 (Expo SDK) no longer auto-includes @types/node; the lib/*.test.ts
    // files use node: built-ins (node:test, Buffer, ...), so request it explicitly.
    "types": ["node"]
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
