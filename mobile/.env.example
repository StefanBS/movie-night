# Dev default. In Expo dev the app auto-derives the dev machine's LAN host from
# Expo (see lib/api.ts resolveApiBaseUrl), so a physical phone works without
# editing this. Set an explicit non-localhost URL to override (staging/prod).
EXPO_PUBLIC_API_URL=http://localhost:8080
